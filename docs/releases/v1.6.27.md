**Fixes**
- Raise Home Assistant minimum version to 2026.3.0 in hacs.json (required for in-tree brand assets)
- Pass config_entry correctly in the options flow
- Use Home Assistant valid_entity_id instead of a custom regex
- Replace the global call_service listener with automation/scene reload, core config reload, and script/group registry rescan listeners
- Rescan when group and other relevant components load via a shared domain list
- Restore Ignored clears ignored state in the issue registry, not just local storage
- Auto-replace uses public async_update only; private config-view fallbacks removed
